import React, { useState } from 'react';
import { ChevronRight, ChevronLeft } from 'lucide-react';

const questions = [
  // ... (question array as defined earlier)
];

const CRMAssessment = () => {
  // ... (component code as defined earlier)
};

export default CRMAssessment;
